# Supabase — powers the live "tag people in photos" sync.
# These are PUBLIC, RLS-protected keys (safe to ship in the client bundle).
# The app falls back to the baked-in defaults in src/config.ts if these are unset,
# so you only need them to point the app at a different Supabase project.
VITE_SUPABASE_URL=https://nbbfmssnlvgfemdizwxh.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_79CFOHXx8Wvr6uZzzXyPhw_NNS0yoyT
